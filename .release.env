# Release config for .github/workflows/release.yml and scripts/release.py
# (release-kit v2). Unquoted KEY=VALUE lines only; see scripts/release.py.
BINS=brake
CRATE_DIR=.
FEATURES=mcp
EXTRA_TARGETS=x86_64-pc-windows-msvc
PUBLISH_CRATE=true
FORMULA_NAME=brake
FORMULA_CLASS=Brake
FORMULA_LICENSE=MIT
FORMULA_DESC=A brake on breaking API changes: checks API contracts for backward compatibility
